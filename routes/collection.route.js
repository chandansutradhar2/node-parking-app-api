const express = require("express");
const { route } = require("./users.route");
const router = express.Router();

// router.get("/all", (req, res) => {
//   res.send("all collection list");
// });

module.exports = router;
